<view bind:tap="onClickError" class="div-error" wx:if="{{show}}">
    <image class="img-tip" src="/image/all/ic_alarm.png"></image>
    <text class="tip-error-text">{{errorMsg}}</text>
    <image class="img-arrow" src="/image/all/ic_close.png" wx:if="{{false}}"></image>
</view>
